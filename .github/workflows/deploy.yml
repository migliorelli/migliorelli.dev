name: Deploy to VPS

on:
  push:
    branches:
      - deploy

jobs:
  deploy:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out code 📩
        uses: actions/checkout@v4

      - name: Log in to Docker Hub 🔐
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push to Docker Hub 🔐
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: miguelmib/migliorelli.dev:latest

      - name: Deploy to VPS 🚀
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd ${{ secrets.DIRECTORY }}
            git pull origin deploy
            docker compose down
            docker compose pull
            docker compose up -d --build

      # - name: Log in to Docker Hub 🔐
      #   run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      # - name: Build Docker image 🏗️
      #   run: |
      #     docker build -t --no-cache migliorelli.dev:latest .
      #     docker save migliorelli.dev:latest | gzip >  migliorelli.dev.tar.gz

      # - name: Set up SSH 🔑
      #   uses: webfactory/ssh-agent@v0.7.0
      #   with:
      #     ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      # - name: Copy Docker image to VPS 📦
      #   run: scp -o StrictHostKeyChecking=no migliorelli.dev.tar.gz ${{ secrets.USER }}@${{ secrets.HOST }}:~/ migliorelli.dev.tar.gz

      # - name: Deploy to VPS 🚀
      #   run: |
      #     ssh -o StrictHostKeyChecking=no ${{ secrets.USER }}@${{ secrets.HOST }} << 'EOF'
      #       docker load < ~/ migliorelli.dev.tar.gz
      #       cd ${{ secrets.DIRECTORY }}
      #       docker compose down
      #       docker compose up -d
      #     EOF
