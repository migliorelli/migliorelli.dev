name: Deploy to VPS

on:
  push:
    branches:
      - deploy

jobs:
  deploy:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out code 📩
        uses: actions/checkout@v4

      - name: Set up SSH 🔑
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Deploy to VPS 🔥
        run: ssh -o StrictHostKeyChecking=no ${{ secrets.USER }}@${{ secrets.HOST }} ' cd ${{ secrets.DIRECTORY }} && git fetch --all && git reset --hard origin/deploy && git pull origin deploy && docker compose down && docker compose build --no-cache && docker compose up -d '
